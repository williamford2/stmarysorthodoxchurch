export interface Bulletin {
  title: string;
  date: string; // ISO date string
  filename: string; // file in /public/bulletins/
}

export const bulletins: Bulletin[] = [
  // Placeholder — bulletin PDFs to be uploaded by parish
  // Add bulletin entries here once PDFs are provided
];
